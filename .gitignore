# dependencies
node_modules/

# environment / secrets
.env
.env.local
.env.*.local

# production credentials — MUST never be committed (rotate if they ever were)
data/prod-secrets.txt
data/prod-admin-pass.txt
data/*-pass.txt
data/*-secrets.txt

# SQLite database — local data with password hashes / session tokens.
# Never commit it (see DEPLOY.md).
data/sqlite.db
data/sqlite.db-shm
data/sqlite.db-wal
data/*.sqlite.db*

# legacy KV JSON dumps (migration sources / backups — contain user data)
data/kv*.json

# netlify local state
.netlify/
.netlify/state/

# logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# editor clutter
.vscode/
.idea/

# misc
*.tmp

# old marketing mockups / leftover drafts (kept on disk, not in the repo)
shop/COACH-OS-The-Operating-System-for-Fitness-Coaches*

# agent tooling worktrees — contain a FULL project copy incl. data/prod-secrets.txt
.kilo/